import { ChunkDirectiveDirective } from './chunk-directive.directive';

describe('ChunkDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChunkDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
